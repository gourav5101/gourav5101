pipeline {
  agent any
  stages {
    stage ('build') {
      steps {
        echo 'this is build'
        sh 'uname -a'
      }
    }
    stage ('test') {
      steps {
        echo 'this is tesr'
      }
    }
  }
}