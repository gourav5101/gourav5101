pipeline {
  agent any
  stages {
      stage{
        steps('build') {
          echo 'this is build'
        }
      }
    stage{
        steps('test') {
          echo 'this is test'
        }
      }
    }
  }
}